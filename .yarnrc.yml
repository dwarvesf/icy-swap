# yarn.lock is Berry (v6) format, so installs must run Yarn 3+, and PnP breaks
# connectkit/walletconnect (undeclared peer deps). node-modules linker matches
# how the lockfile was generated and how Vercel builds it.
nodeLinker: node-modules
